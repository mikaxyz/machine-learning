{
  "private": true,
  "name": "xyz-mika-machine-learning",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "elm make src/Main.elm --output dist/main.js",
    "test": "elm-test",
    "train": "npm run build && ./index.js train --data-path=.data/mnist_train.csv --data-limit=500 --neurons=20",
    "test-model": "npm run build && ./index.js test --model-path=.models/mnist_train_csv,rate=0.01,fn=tanh,seed=42,layers=20,limit=500.json --data-path=.data/mnist_test.csv --data-limit=1000"
  },
  "author": "Mika Jauhonen",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.5",
    "elm-test": "^0.19.1-revision7",
    "yargs": "^17.7.2"
  },
  "dependencies": {
    "@andrewmacmurray/elm-concurrent-task": "^1.1.0"
  }
}
